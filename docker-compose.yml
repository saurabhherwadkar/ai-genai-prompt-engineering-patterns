version: "3.9"
services:
  prompt-api:
    build: .
    container_name: prompt-engineering
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./config:/app/config:ro
    restart: unless-stopped
