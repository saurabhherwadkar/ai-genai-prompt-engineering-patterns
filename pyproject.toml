[tool.poetry]
name = "ai-genai-prompt-engineering-patterns"
version = "1.0.0"
description = "Production prompt engineering patterns with versioning, A/B testing, CoT/few-shot, testing framework, and compression"
authors = ["Saurabh Herwadkar"]
readme = "README.md"
license = "MIT"
packages = [{include = "prompt_engineering", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.12,<3.14"
anthropic = "^0.49.0"
openai = "^1.61.0"
fastapi = "^0.115.6"
uvicorn = {extras = ["standard"], version = "^0.34.0"}
pydantic = "^2.10.0"
pydantic-settings = "^2.7.0"
pyyaml = "^6.0.2"
structlog = "^24.4.0"
httpx = "^0.28.0"
python-dotenv = "^1.0.1"
jinja2 = "^3.1.5"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"
pytest-asyncio = "^0.25.0"
pytest-cov = "^6.0.0"
pytest-mock = "^3.14.0"
ruff = "^0.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--cov=src/prompt_engineering --cov-report=term-missing --cov-report=html"

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "S", "B", "A", "COM", "C4", "PT"]
ignore = ["S101", "COM812"]

[tool.ruff.lint.isort]
known-first-party = ["prompt_engineering"]
